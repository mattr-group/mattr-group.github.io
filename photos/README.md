Put member photographs here, named exactly as the `photo` column in the sheet
says (for example `pete.jpg`).

Square crop, 400 by 400 pixels is plenty, JPEG at moderate quality. The page
displays them at 48 pixels, so anything larger only costs load time.

A row with an empty `photo` cell gets a generated pattern instead, so the page
never looks broken while photographs are still being collected.
